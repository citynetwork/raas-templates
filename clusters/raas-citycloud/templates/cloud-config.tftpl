[Global]
auth-url=${ auth_url }
username=${ username }
password=${ password }
region=${ region }
tenant-name="${ tenant_name }"
domain-name=${ domain_name }

[LoadBalancer]
use-octavia=true
subnet-id=${ subnet_id }
floating-network-id=${ floating_network_id }